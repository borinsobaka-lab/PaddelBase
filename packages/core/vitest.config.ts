import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    globals: true,
    include: ['src/**/*.test.ts'],
    /**
     * Интеграционные тесты работают против одной базы и чистят её перед каждым
     * тестом. При параллельном запуске файлов они затирали бы данные друг друга,
     * поэтому файлы идут последовательно. Чистые тесты от этого почти не
     * замедляются — весь пакет проходит меньше чем за пару секунд.
     */
    fileParallelism: false,
  },
});
