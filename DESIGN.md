# PaddelBase — эстетическое направление

Одно направление на всё приложение. Токены живут в
`apps/web/src/app/globals.css`, примитивы — в `apps/web/src/components/ui.tsx`.
Этот файл объясняет, **почему** именно так; код объясняет, **как**.

Правило одно: если решение нужно изменить, оно меняется здесь и в токенах.
Локальная правка в компоненте — это второй способ сделать то же самое, а два
способа расходятся быстрее, чем кажется.

---

## 1. Сцена

Игрок стоит у стеклянного корта в Батуми, вечер, прожекторы, телефон в руке.
Он решает одну из трёх задач:

1. Куда я записан?
2. Куда ещё можно записаться?
3. Что от меня сейчас требуется?

Отсюда всё остальное. **Тема светлая** — не «на всякий случай», а потому что
экран читают на улице днём и под прожектором вечером; тёмная тема на солнце
превращается в зеркало. Ширина проектирования — **390 px**.

Продукт держится на двух регистрах, и они не смешиваются:

| Регистр | Что это | Чем выражен |
| --- | --- | --- |
| Тёплый, общительный | лента, «ищем четвёртого», комьюнити | Onest, зелень корта, мягкая тень |
| Точный, официальный | уровень, счёт, очки, дельта | JetBrains Mono, табличные колонки |

Рейтинг — то, о чём игроки будут спорить. Он обязан выглядеть посчитанным.

---

## 2. Палитра

Стратегия — **сдержанная**: подкрашенные нейтралы плюс один акцент, который
занимает меньше десятой части экрана. Насыщенный цвет здесь не украшение, а
сообщение, и его должно быть мало, иначе он перестаёт значить что-либо.

Цвет задан в **OKLCH**. У неё светлота воспринимаемая: шаг «на два процента
светлее» на глаз одинаков и для серого, и для зелёного, чего про HEX сказать
нельзя.

### Поверхности

Один тон, **H 175**, хроматичность не выше 0.011. Это стекло корта, увиденное
с торца. Важное: нейтралы подкрашены **в сторону собственного акцента**, а не
в дежурный холодный синий — разница почти не называется словом, но экран
перестаёт выглядеть чужим по отношению к зелени.

| Токен | OKLCH | ≈ HEX | Роль |
| --- | --- | --- | --- |
| `canvas` | `0.968 0.006 175` | `#f1f6f4` | фон страницы |
| `surface` | `1 0 0` | `#ffffff` | карточка |
| `sunken` | `0.945 0.009 175` | `#e7efec` | поле ввода, дорожка переключателя |
| `border` | `0.898 0.011 175` | `#d6e0dd` | волосяной разделитель внутри карточки |
| `border-strong` | `0.640 0.018 175` | `#7f8d89` | граница элемента управления |

`border` декоративен. `border-strong` — граница, по которой опознают элемент
управления, поэтому он держит **3.05 : 1** к холсту: заливка поля (`sunken`
против `surface`) даёт всего 1.16 : 1 и опознать поле сама по себе не может.

### Текст — четыре ступени

| Токен | OKLCH | ≈ HEX | Контраст к `canvas` | Роль |
| --- | --- | --- | --- | --- |
| `text` | `0.268 0.021 175` | `#1b2925` | 13.74 : 1 | основное |
| `text-secondary` | `0.452 0.019 175` | `#4b5955` | 6.67 : 1 | сопровождающее |
| `muted` | `0.518 0.017 175` | `#5f6c68` | 5.03 : 1 | метаданные, плейсхолдер |
| `faint` | `0.612 0.016 175` | `#77837f` | 3.41 : 1 | только графика: шевроны, разделители |

`muted` темнее, чем хочется глазу, и это сознательно. Светло-серый текст «для
воздуха» на подкрашенном почти-белом — самая частая причина, по которой
интерфейс тяжело читать; здесь он держит 4.5 : 1 на всех трёх поверхностях,
включая `sunken`. `faint` не бывает текстом никогда — только стрелка, точка,
пунктир.

### Акценты

| Токен | OKLCH | ≈ HEX | Роль |
| --- | --- | --- | --- |
| `accent` | `0.512 0.118 158` | `#067a4c` | зелень корта: действие, выбор, рост |
| `accent-ink` | `1 0 0` | `#ffffff` | текст на зелени, 5.37 : 1 |
| `accent-soft` | `0.945 0.032 158` | `#dcf4e5` | заливка корта в составе |
| `ball` | `0.885 0.166 113` | `#dae452` | **только «сейчас твой ход»** |
| `ball-ink` | `0.285 0.062 122` | `#252f06` | текст на мяче, 10.27 : 1 |
| `ball-soft` | `0.972 0.045 110` | `#f7f9d7` | фон блока, ждущего действия |
| `warn` | `0.522 0.108 62` | `#95591a` | что-то идёт не так, действия не ждут |
| `danger` | `0.508 0.176 27` | `#b52a27` | необратимое, отказ, падение уровня |

**Жёлтый мяча — единственный насыщенный цвет в приложении.** На настоящем
корте это единственная насыщенная точка: всё остальное — стекло, зелёный пол,
белая разметка. Здесь он значит ровно одно и появляется ровно там, где от
игрока чего-то ждут: плашка «требует вас», карточка отклика, панель ввода
счёта. Использовать его как «просто жёлтый» — ошибка, которую видно сразу.

### Контраст

Все 28 пар «текст на фоне», встречающиеся в приложении, проверены численно и
проходят: 4.5 : 1 для текста, 3 : 1 для крупного текста и опознавательной
графики. Проверка воспроизводима — `docs/contrast.mjs`; запускать её нужно
после любой правки палитры.

---

## 3. Шрифты

**Onest** для текста, **JetBrains Mono** для чисел.

Пара стоит на честной оси контраста — гуманистический гротеск против
механического моноширинного. Два похожих гротеска рядом смотрятся как ошибка
вёрстки; эти двое не спорят, потому что делают разное.

### Onest — текст

Кириллица нарисована вместе с латиницей, а не пристроена к ней; на 13–15 px
это заметно на каждом экране, а весь интерфейс здесь по-русски. Переменный,
400–700, один файл на начертание.

### JetBrains Mono — числа

Всё, что игрок может оспорить, набирается моноширинным: **уровень, счёт,
очки, дельта, время, места в таблице**. Три причины, и ни одна не про красоту:

1. Колонки выравниваются по построению, а не костылём `tabular-nums`.
2. Значение не дёргается при изменении — важно для живой дуги уровня.
3. Вид шрифта сам говорит «посчитано», а не «показалось». Для рейтинга,
   вокруг которого будут споры, это половина доверия.

Утилита — `.figure`. Правило: **число, у которого есть смысл, — моноширинное;
число внутри фразы — нет.** «19:00» в карточке матча — `.figure`; «вс, 9
августа» рядом — обычный текст. Фраза целиком в моноширинном — ошибка: в
«осталось 42 символа» моноширинным набирается только «42».

### Знак лари

В Onest нет `₾` (U+20BE): браузер подставлял его из системного шрифта, и рядом
с цифрами Onest он выглядел чужим. Поэтому взнос пишется словом — «20 лари».
Правило общее: **символ, которого нет в Onest, в интерфейс не попадает.**
Проверять — `document.fonts.check` врёт (он смотрит на диапазон, а не на
глиф); сравнивать надо ширину знака в Onest и в заведомо другом шрифте: совпали
— значит рисует не Onest.

### Шкала кеглей

Шаг ≈ 1.2 от 15 px. Кегли различимы на глаз, а не отличаются на пиксель.

| Утилита | px | Интерлиньяж | Трекинг | Где |
| --- | --- | --- | --- | --- |
| `text-caption` | 11 | 1.3 | 0 | подписи, плашки |
| `text-small` | 13 | 1.45 | 0 | метаданные, подсказки |
| `text-body` | 15 | 1.55 | 0 | основной текст |
| `text-title` | 18 | 1.35 | 0 | заголовок секции |
| `text-h2` | 22 | 1.25 | −0.01em | заголовок экрана второго уровня |
| `text-h1` | 27 | 1.15 | −0.02em | заголовок экрана |
| `text-display` | 34 | 1.05 | −0.022em | счёт матча |
| `text-hero` | 44 | 1 | −0.025em | уровень в профиле |

Трекинг ужимается по мере роста кегля и никогда не заходит за −0.04em: дальше
буквы начинают слипаться, и это читается как небрежность, а не как замысел.

### Подпись без капслока

Мелкая разреженная заглавная строка над каждой секцией — самый узнаваемый
штамп сгенерированных интерфейсов. В кириллице она вдобавок читается заметно
хуже латиницы: у русских прописных нет выносных элементов, за которые
цепляется глаз, и слово превращается в прямоугольник.

Поэтому `.label` — 11 px, вес 600, обычный регистр, цвет `muted`. Иерархию
держат те же три рычага: кегль, насыщенность, цвет.

---

## 4. Шкала отступов

База — **4 px**. Никаких значений вне сетки в раскладке.

| Шаг | px | Роль |
| --- | --- | --- |
| `1` | 4 | зазор между иконкой и подписью |
| `2` | 8 | внутри элемента управления |
| `3` | 12 | между строками списка, зазор ленты |
| `4` | 16 | поля карточки, поля страницы |
| `5` | 20 | между карточками внутри секции |
| `6` | 24 | между блоками внутри экрана |
| `8` | 32 | между секциями |
| `12` | 48 | между крупными зонами, отбивка сверху |

Единственное исключение — **оптические поправки меньше 4 px**: `mt-0.5` под
выравнивание базовых линий, `py-0.5` в плашке, `gap-1.5` между иконкой и
цифрой. Это не раскладка, а подгонка знака к знаку. В раскладке половинных
шагов нет.

**Ритм неровный намеренно.** Внутри секции — 12, между секциями — 32. Ровная
сетка везде звучит как «никто не решал».

Опорные значения: поля карточки **16**, зазор списка **12**, между секциями
**32**, отбивка экрана сверху **20**.

---

## 5. Глубина, слои, движение

**Одна стратегия глубины:** кольцо плюс мягкая тень (`shadow-raise` →
`shadow-lift` → `shadow-float`). Карточка стоит на тени, без рамки. Граница
остаётся там, где действительно разделяет: поля ввода, волосяные линии в
списках, пунктир свободного места. Смешивать нельзя — как только рядом
окажутся карточка с рамкой и карточка с тенью, обе выглядят случайными.

**Вложенных карточек не бывает.** Если внутри карточки нужна группа — это
строки с волосяной линией или утопленный колодец (`sunken`), но не вторая
карточка со своей тенью.

**Слои — именованная лестница**, а не числа наугад:
`sticky 10 → nav 20 → fab 25 → overlay 30 → sheet 35`.

**Движение** — только `transform` и `opacity`, только `ease-out` с
экспоненциальной кривой, без пружин. Анимируется то, что действительно
изменилось: приход блока «требует вас» и отрисовка дуги уровня. Одинаковый
въезд на каждую секцию — это не движение, а тик.

Конечное состояние всегда является состоянием по умолчанию: если анимация не
запустится (фоновая вкладка, headless-рендер, `prefers-reduced-motion`),
экран уже собран и виден.

---

## 6. Сигнатура

**Состав матча — миниатюрный корт** (`components/CourtLineup.tsx`). Падел
всегда 2 × 2 на корте, разделённом сеткой; ряд кружков с инициалами не
показывает ни пар, ни с какой стороны свободно место. Свободные места —
пунктирные ячейки: пустота здесь содержательна, это приглашение, а не
отсутствие данных.

**Дуга надёжности вокруг уровня** (`components/LevelDial.tsx`) отвечает на
вопрос «насколько этой цифре можно верить». Без неё уровень новичка и
ветерана выглядят одинаково весомо — ровно та ошибка, которую рейтинговая
система обязана не допускать.

---

## 7. Что сюда не попадёт

Записано, чтобы не вернуться по инерции:

- Цветная полоса-акцент слева у карточки или уведомления.
- Градиент в тексте.
- Стекло и блюр ради вида. Блюр только там, где под панелью реально едет
  контент.
- Ряд одинаковых плиток «иконка, заголовок, две строки».
- Мелкая разреженная заглавная надстрочная подпись над каждой секцией.
- Нумерация секций 01 / 02 / 03 там, где это не настоящая последовательность.
- Кремово-песочный фон. Нейтралы подкрашены в сторону корта, а не в «тепло по
  умолчанию».
- Системный шрифтовой стек. Он означает, что шрифт не выбирали.

---

## 8. Где что лежит

| Слой | Файл |
| --- | --- |
| Токены, `@font-face`, базовые утилиты | `apps/web/src/app/globals.css` |
| Файлы шрифтов | `apps/web/public/fonts/` |
| Примитивы | `apps/web/src/components/ui.tsx` |
| Сигнатура | `CourtLineup.tsx`, `LevelDial.tsx`, `LevelStrip.tsx` |
| Карточки сущностей | `MatchCard.tsx`, `TournamentCard.tsx`, `PostCard.tsx` |
| Каркас | `AppShell.tsx`, `BottomNav.tsx`, `CreateButton.tsx` |
| Проверка контраста | `docs/contrast.mjs` |

`AppShell` владеет нижним отступом: плавающая кнопка стоит над панелью, и если
про неё забыть, она накроет последнюю карточку списка.
