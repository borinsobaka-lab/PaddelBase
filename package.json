{
  "name": "paddelbase",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "db:generate": "pnpm --filter @paddelbase/db generate",
    "db:migrate": "pnpm --filter @paddelbase/db migrate:dev",
    "db:deploy": "pnpm --filter @paddelbase/db migrate:deploy",
    "db:seed": "pnpm --filter @paddelbase/db seed"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
